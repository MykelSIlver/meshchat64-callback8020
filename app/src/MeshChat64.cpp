#include <sailfishapp.h>

int main(int argc, char *argv[])
{
    // Standard SailfishApp entry point. All logic lives in QML;
    // the app is deliberately a thin shell around the WebView.
    return SailfishApp::main(argc, argv);
}
